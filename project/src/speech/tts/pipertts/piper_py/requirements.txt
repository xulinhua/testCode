# Piper语音合成项目依赖包
# 针对Jetson Orin NX Super (Ubuntu 22.04 + Python 3.10.12 + JetPack 6.2)优化
# 版本范围针对Jetson平台特定优化，确保兼容性

# 核心依赖
onnxruntime>=1.14.0               # 使用CPU版本，ARM64架构兼容
# onnxruntime-gpu>=1.14.0         # GPU版本不支持ARM64架构，已注释
piper-phonemize>=1.1.0,<2.0.0     # 音素转换库，支持1.x版本
numpy>=1.22.0,<2.0.0             # 确保使用numpy 1.x系列，避免numpy 2.0的兼容性问题

# 音频处理
soundfile>=0.10.0,<1.0.0         # WAV文件读写，使用稳定版本
pyaudio>=0.2.11,<0.3.0           # 音频设备操作，保持向后兼容

# 工具库
pyyaml>=6.0.0,<7.0.0             # YAML配置解析，稳定版本系列
click>=8.0.0,<9.0.0              # 命令行工具，8.x稳定版本

# 可选：GUI界面支持（注释掉，默认不安装）
# pyqt5>=5.15.0,<6.0.0            # GUI界面，Qt5稳定版本
# pyqt5-tools>=5.15.0,<6.0.0      # Qt工具，与pyqt5版本对应

# 可选：硬件监控
psutil>=5.9.0,<6.0.0              # 系统资源监控，5.x稳定版本

# 开发依赖（仅在开发环境安装）
# pytest>=7.0.0,<8.0.0            # 测试框架，7.x稳定版本
# pytest-cov>=4.0.0,<5.0.0         # 测试覆盖率，4.x版本
# black>=22.0.0,<24.0.0            # 代码格式化，22-23稳定版本
# flake8>=5.0.0,<7.0.0             # 代码检查，5-6稳定版本